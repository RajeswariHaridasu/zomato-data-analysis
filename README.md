# Power BI Dashboard

The original `.pbix` file is not included in this repository because of GitHub file-size limitations.

Export a dashboard screenshot from Power BI and save it as:

`../images/dashboard.png`

The screenshot will be displayed in the main project README.
